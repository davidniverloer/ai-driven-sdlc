# Legacy Create Process Tree Command

This command name is retained as a compatibility alias.

Use the current command specification instead:

```text
docs/commands/create-process.md
```

Current trigger phrase:

```text
Create an ai-driven-sdlc process named: <name>
```
