# Legacy Process Tree Path

`process-trees/` is retained as a compatibility path.

The canonical process directory is now:

```text
processes/
```

Use these files for new work:

```text
processes/README.md
processes/default.md
processes/templates/process-template.md
```

Do not add new Process Tree files here. New or updated execution maps should be
created under `processes/`.
