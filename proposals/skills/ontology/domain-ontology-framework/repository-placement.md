# Repository Placement Report

## Selected Capability

`ontology`

## Selected Repository Path

```text
skills/ontology/domain-ontology/
```

## Placement Rationale

The proposed skill belongs in `ontology` because it establishes authoritative
domain language before feature, requirement, UX, or architecture design.

The proposed canonical path matches the existing Process Tree location for:

```text
PT-004 Canonical Domain Ontology
```

## Affected Assets

- `process-trees/default.md`
- `skills/ontology/README.md`
- Future canonical path `skills/ontology/domain-ontology/`
- Upstream `skills/strategy/concept-interrogation/`

## Collision Check

No canonical package currently exists at:

```text
skills/ontology/domain-ontology/
```

The location is already reserved by the canonical Process Tree. Promotion would
create the initial canonical package for `PT-004`.

## Placement Status

Approved
