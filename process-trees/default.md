# Legacy Default Process Tree

This file is retained as a compatibility pointer.

The canonical default process is now:

```text
processes/default.md
```

Future agents must inspect and update `processes/default.md` for current
process governance.
